<?php

/**
 * Shared config for all suites (unit/functional/acceptance). Suite-specific
 * configs (e.g. unit.php) can override anything declared here. Empty by
 * default — HumHub's defaults are sufficient for our pure-PHP unit tests.
 */
return [];
