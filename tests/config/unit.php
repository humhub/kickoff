<?php

/**
 * Unit-suite overrides on top of common.php. Empty by default — the default
 * HumHub test config in `@humhubTests/codeception/config/config.php` covers
 * what our pure-PHP unit tests need.
 */
return [];
